<%= form_for(son) do |f| %>
  <% if son.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(son.errors.count, "error") %> prohibited this son from being saved:</h2>

      <ul>
      <% son.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :s_name %>
    <%= f.text_field :s_name %>
  </div>

  <div class="field">
    <%= f.label :father_id %>
    <%= f.text_field :father_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
